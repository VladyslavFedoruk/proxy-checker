# URL Monitor — Admin Panel

Админ-панель для мониторинга URL-адресов через прокси с отслеживанием HTTP кодов ответа.

## Возможности

- 🔗 **Мониторинг URL** — добавляйте любые URL для отслеживания
- 🌐 **Поддержка прокси** — проверяйте URL через разные прокси/гео
- 📊 **Коды ответа** — отслеживание 200, 404, 500 и других статусов
- ⏱ **Время отклика** — измерение скорости ответа в миллисекундах
- 📈 **История проверок** — просмотр истории для каждого URL
- 🔄 **Автоматический мониторинг** — фоновая проверка с настраиваемым интервалом

## Быстрый старт

### 1. Установка зависимостей

```bash
cd proxy-checker
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Запуск сервера

```bash
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Открыть админ-панель

Откройте в браузере: [http://localhost:8000](http://localhost:8000)

## Использование

### Добавление прокси

1. Перейдите на вкладку "Прокси"
2. Заполните данные:
   - **Название** — понятное имя (например, "US Proxy")
   - **Гео** — код страны (US, DE, RU и т.д.)
   - **Протокол** — HTTP, HTTPS или SOCKS5
   - **Хост и порт** — адрес прокси сервера
   - **Логин/пароль** — при необходимости авторизации

### Добавление URL для мониторинга

1. На главной вкладке "URL мониторинг"
2. Введите URL для отслеживания
3. Выберите прокси (или оставьте "Напрямую")
4. Укажите интервал проверки в секундах
5. Нажмите "Добавить URL"

### Функции

- **Проверить сейчас** ✓ — мгновенная проверка URL
- **История** 🕐 — просмотр всех проверок
- **Вкл/Выкл** ⏸/▶ — управление мониторингом
- **Удалить** 🗑 — удаление из системы

## API Endpoints

### Прокси
- `GET /api/proxies` — список прокси
- `POST /api/proxies` — создать прокси
- `PUT /api/proxies/{id}` — обновить
- `DELETE /api/proxies/{id}` — удалить

### URL мониторинг
- `GET /api/urls` — список URL
- `POST /api/urls` — добавить URL
- `PUT /api/urls/{id}` — обновить
- `DELETE /api/urls/{id}` — удалить
- `POST /api/urls/{id}/check` — проверить сейчас
- `GET /api/urls/{id}/history` — история проверок

### Статистика
- `GET /api/stats` — общая статистика

## Структура проекта

```
proxy-checker/
├── app/
│   ├── __init__.py
│   ├── main.py          # FastAPI приложение
│   ├── database.py      # Настройка базы данных
│   ├── models.py        # SQLAlchemy модели
│   ├── schemas.py       # Pydantic схемы
│   ├── checker.py       # Логика проверки URL
│   └── templates/
│       └── index.html   # Админ-панель
├── requirements.txt
└── README.md
```

## Технологии

- **Backend**: FastAPI, SQLAlchemy, APScheduler
- **Database**: SQLite (aiosqlite)
- **HTTP Client**: httpx (async)
- **Frontend**: Vanilla JS, современный CSS

